/* @const Get the current_user contacts */
const GET_CONTACTS = 'get_contacts';

/* @const Change Current Contact */
const CHANGE_CONTACT = 'change_contact';

export default {
  GET_CONTACTS,
  CHANGE_CONTACT
};
